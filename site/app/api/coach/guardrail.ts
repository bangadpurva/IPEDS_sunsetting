import type { CoachEvidence } from './coach';
const unsafe=[/guarantee(?:d|s)?/i,/definitely (?:will|is)/i,/you will (?:get|earn|be admitted|succeed)/i,/best choice for you/i,/no risk/i];
export function verifyCoachAnswer(answer:string,evidence:CoachEvidence[]){const allowed=new Set(evidence.flatMap(e=>[e.jobGrowth,e.programChange].filter((n):n is number=>typeof n==='number').map(n=>Math.abs(n).toFixed(1))));const percentages=[...answer.matchAll(/[+-]?(\d+(?:\.\d+)?)%/g)].map(m=>Number(m[1]).toFixed(1));const unsupported=percentages.filter(value=>!allowed.has(value));const issues:string[]=[];if(unsupported.length)issues.push('unsupported_numeric_claim');if(unsafe.some(pattern=>pattern.test(answer)))issues.push('unsafe_confidence');if(answer.length>2500)issues.push('excessive_length');return{passed:issues.length===0,issues}}
